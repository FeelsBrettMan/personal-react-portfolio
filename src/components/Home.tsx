import React from "react";
import {Typography} from "@material-ui/core";

export const Home: React.FC = () =>{
    return(
        <div>
            <Typography variant={'body1'} style={{color: "white"}}>I'm home</Typography>
        </div>
    )
}
